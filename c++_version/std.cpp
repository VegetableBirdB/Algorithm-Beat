//This is an example of the sum of two nums.
//This cpp file refers to standard answer(Brute force algorithm makes the result absolutely right).
//You should create a main function to allow homologous input and output, and then import your function to solve the problem.

#include <bits/stdc++.h>
using namespace std;

int main() {
  int a, b;
  cin >> a >> b;
  cout << a + b << endl;
  return 0;
}